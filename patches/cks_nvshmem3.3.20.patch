diff --git a/src/modules/transport/ibgda/ibgda.cpp b/src/modules/transport/ibgda/ibgda.cpp
index d037ccc..924626c 100644
--- a/src/modules/transport/ibgda/ibgda.cpp
+++ b/src/modules/transport/ibgda/ibgda.cpp
@@ -3709,11 +3709,21 @@ int nvshmemt_init(nvshmem_transport_t *t, struct nvshmemi_cuda_fn_table *table,
         const char *name = ftable.get_device_name(device->dev);
         NVSHMEMI_NULL_ERROR_JMP(name, status, NVSHMEMX_ERROR_INTERNAL, out,
                                 "ibv_get_device_name failed \n");
-        if (!strstr(name, "mlx5")) {
+
+        const char *hca_ifname = getenv("NVSHMEM_IBGDA_HCA_IFNAME");
+        bool device_supported = false;
+
+        if (hca_ifname) {
+            device_supported = strstr(name, hca_ifname) != NULL;
+        } else {
+            device_supported = strstr(name, "mlx5") != NULL || strstr(name, "ibp") != NULL;
+        }
+
+        if (!device_supported) {
             ftable.close_device(device->context);
             device->context = NULL;
-            NVSHMEMI_WARN_PRINT("device %s is not enumerated as an mlx5 device. Skipping...\n",
-                                name);
+            NVSHMEMI_WARN_PRINT("device %s is not supported (expected HCA interface: %s). Skipping...\n",
+                                name, hca_ifname ? hca_ifname : "mlx5 or ibp");
             continue;
         }
 
